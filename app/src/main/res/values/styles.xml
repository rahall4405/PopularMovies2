<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="EditTextDetail">
        <item name="android:editTextColor">@color/DetailTextColor</item>
        <item name="android:textSize">@dimen/detail_text_size</item>
        <item name="android:layout_height">@dimen/detail_textview_layout_height</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:layout_weight">@string/detail_textview_weight</item>
    </style>

</resources>
